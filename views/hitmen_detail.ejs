<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Hitman Detail | Spy Agency</title>
    <link href="/assets/css/main.css" rel="stylesheet">
</head>
<body>
    <%- include('./layout/topbar') -%>

    <div class="container mt-10 mx-auto px-10">
        <div class="text-center">
            <h1 class="text-3xl font-bold mb-10">Hitman Detail</h1>
        </div>
        <div>
            <label class="text-2xl font-bold">Name:&nbsp;</label><span class="text-2xl font-extralight"><%= hitman.name %></span>
        </div>
        <div>
            <label class="text-2xl font-bold">Email:&nbsp;</label><span class="text-2xl font-extralight"><%= hitman.email %></span>
        </div>
        <div>
            <label class="text-2xl font-bold">Description:&nbsp;</label><span class="text-2xl font-extralight"><%= hitman.description %></span>
        </div>
        <div>
            <label class="text-2xl font-bold">Manager:&nbsp;</label><span class="text-2xl font-extralight"><%= hitman.manager %></span>
        </div>
        <div>
            <label class="text-2xl font-bold">Status:&nbsp;</label>
            <div class="badge <%= (hitman.active == 1 ? 'badge-success' : 'badge-error') %> gap-2">
                <span><%= hitman.active == 1 ? 'active' : 'inactive' %></span>
            </div>
        </div>
        <div>
            <label class="text-2xl font-bold">Created At:&nbsp;</label><span class="text-2xl font-extralight"><%= hitman.created_at %></span>
        </div>
        <% if(hitman.active == 1) { %>
            <div class="mt-10">
                <form action="/hitmen/<%= hitman.id %>/deactivate" method="post">
                    <button type="submit" class="btn btn-sm btn-error">Deactivate</button>
                </form>
            </div>
        <% } %> 
        <% if(user.id_type == 1 && user.id == 1) { %>
        <h1 class="text-3xl font-bold my-5">Other Hitmen</h1>
        <div class="overflow-x-auto">
            <table class="table">
                <thead>
                    <tr>
                    <th>Hitman</th>
                    </tr>
                </thead>
                <tbody>
                    <% for(let i = 0; i < hitmen.length; i++) { %>
                        <tr>
                            <td>
                                <a href="/hitmen/<%= hitmen[i].id %>" class="btn btn-sm"><%= hitmen[i].name %></a>
                            </td>
                        </tr>
                    <% } %>
                </tbody>
            </table>
        </div>
        <% } %>
    </div>
</body>
</html>